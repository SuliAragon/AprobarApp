{
  "name": "aprobarapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/sync-temario.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/sync-temario.mjs",
    "test": "node --test scripts/lib/**/*.test.mjs",
    "build": "astro build",
    "build:github": "PUBLIC_SITE_URL=${PUBLIC_SITE_URL:-https://example.github.io} PUBLIC_BASE_PATH=${PUBLIC_BASE_PATH:-/AprobarApp/} astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.12.8"
  }
}
